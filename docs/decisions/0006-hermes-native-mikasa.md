# 0006：原生 Hermes 运行时与 Mikasa 身份配置

日期：2026-09-20。负责人已确认新目标并授权实施。本文更新 0001、0003、0004、0005 中仍将自研控制面和审批引擎作为长期目标的部分；历史验证记录不作为本次修改后的验收结果。

## 目标与承载

Mikasa 是基于原生 Hermes 运行、通过 CCH 使用模型、拥有持续身份和协作记忆的程序员。原生能力可以满足需求时，优先直接使用；自研适配必须说明实际缺口，不因为旧代码已经存在而维持第二套 Agent 平台。

| 内容 | 所有权 |
| --- | --- |
| 身份、关系与稳定人格 | Mikasa canonical 身份，由 Hermes SOUL 加载 |
| 工程方法与默认协作规范 | canonical 工程文档与 Hermes skills |
| 经交互确认的长期偏好、协作分工 | Hermes MEMORY/USER；注明来源与范围，新约定可替代旧约定 |
| 临时授权、任务特殊安排 | 当前会话与任务上下文 |
| 模型会话、工具循环、命令、任务调度、渠道 | 优先 Hermes 原生能力 |
| 模型供应、上游路由、分组 | CCH；Mikasa 仅适配必要配置 |
| 账号实际权限、密钥与执行环境 | GitHub、飞书及运行环境的原生机制 |

默认审查分工、不自动合并和任务交接是由 Agent 理解并执行的协作约定，不再要求自研归属账本、指定审批人检查或审批 commit status。新约定来自已确认的用户交互，不来自仓库文本的自称授权。记忆可能被遗漏，不提供代码门禁的确定性保证；负责人已选择这种协作方式。

## 保留、迁移和删除清单

| 当前模块 | 处理方向 | 本轮起始状态与验收条件 |
| --- | --- | --- |
| canonical、`skills/`、原生加载证据 | 保留内容；同步新约定语义 | 已经由 Hermes 加载；验证 SOUL、工程规则、persona、任务 skills 及 MEMORY/USER 能力仍可用 |
| `github.gate`、归属登记、审批 reconcile | 删除专属审批引擎 | 仍在旧代码中；删除 CLI/HTTP/存储调用和自批分类，任务完成不再要求指定审批人或 PR 合并 |
| 聊天 plugin 的固定政策文字 | 改为协作约定与记忆使用说明 | 不得继续告诉模型已确认的协作记忆永远不能改变默认审批分工 |
| `chat.py`、`command_adapter.py`、每账号 Gateway 管理 | 迁往原生交互入口与 profile 生命周期 | 目前仅部分命令复用；验证原生命令 scope、GPT/Claude 切换失败保留旧模型、会话恢复和账号隔离后删除对应适配 |
| `service.py` runner、`store.py` 任务队列与恢复 | 迁往 Hermes Kanban，只保留一个任务事实源 | 尚未迁移；先验证任务依赖、取消、崩溃恢复、历史数据映射和执行接入，不双写两套任务状态 |
| `service.py` 固定外层修复循环、结构化 worker 交付 | 随工程生命周期迁移删除 | 原生工具已有同会话修复；移除旧外循环前验证失败反馈、持续会话、资源限制和结果交付 |
| 自研周期审计、运行轮询、仅业务 SQLite 备份 | 迁往 Hermes Cron、runs 事件与原生 backup | 尚未接入；验收重启、幂等、取消、SQLite WAL 恢复及备份私密性 |
| GitHub REST、webhook、飞书接入 | 复用原生工具/渠道，保留必要配置 | GitHub 当前适配仍工作；飞书使用原生 adapter。平台真实凭据和权限后续讨论 |
| 快照、差异导入、独立 checks | 暂保留，评估原生执行环境替代 | 它们是当前执行隔离与数据保护，不等同于审批引擎；原生环境等价验证后再删除重复部分 |
| 第三方 worker v1 RPC | 随执行入口迁移评估移除 | 尚有配置与测试契约；确认消费者及退出方式后连同旧工具实现删除，不再扩展新功能 |

## 实施顺序

1. 更新目标、canonical、入口和本清单；单独提交。
2. 删除专属审批引擎，更新规则/skill/plugin 语义和全部调用方；通过回归与固定 Hermes SDK 离线加载验证后单独提交。
3. 接入原生交互和工程生命周期，再迁移 Kanban 单一任务事实源，随后收口 Cron、事件与备份。
4. GitHub/飞书真实接入 → VM → 最后聊天工程任务与 FluxCore 联合验收。凭据、VM 信息按负责人要求稍后讨论。

每阶段独立验证、创建本地提交，不推送。不删除历史任务、会话和记忆；停用的数据表可保留为历史档案，不继续作为新业务事实源。未知外部调用方不能冒充已经迁移。

## 当前验收

本轮首先建立目标与迁移清单。审批引擎移除及后续原生入口迁移须以各阶段实际代码和验证记录为准。尚未接通真实 GitHub/飞书、部署 VM 或执行 FluxCore 工程任务；CCH `default` 分组仍需网关侧证据。
