# 贡献 Mikasa

## 开始前

先读取 [AGENTS.md](AGENTS.md) 及其指定的三个 canonical 文档。确认本次变更属于身份规则、工程契约、运行设计、集成适配、worker、skill 或部署中的哪一层。

没有明确试点、运行方案或主人决定时，记录候选和待定项，不把方案草图写成已部署事实。不要将真实 token、`auth.json`、模型配置中的密钥、webhook secret 或运行数据提交到仓库。

## 变更边界

- 身份、关系、权限和审批政策只能由主人明确决定后修改。
- 工程规则变更应说明事实来源、影响范围和验证方式。
- 运行实现应保留可审查的配置、日志脱敏和失败恢复路径。
- 默认将 Mikasa 自己或受委派执行者产出的 PR 交项目负责人或指定审查者审查；按当前协作约定执行。本仓库不自动合并。
- 保留其他人的未提交改动，不使用 reset、checkout 或批量覆盖清理工作区。

## 提交前检查

提交应包含已验证、属于本次任务的改动，并使用明确的说明。保留其他人的未提交工作，不包含本地配置、认证或运行状态。验证失败或外部阻塞须如实说明；本地提交不表示 PR 已获批准或允许合并。自动化工具按当前任务授权执行提交，不自动推送。

至少确认：

- 文档链接、canonical 读取顺序和旧身份残留检查通过；
- 新增配置和脚本不包含凭据，并说明副作用；
- 实际运行的检查、未运行的检查和外部阻塞分别记录；
- 变更范围与提交说明一致。

当前核心运行时使用 Python 3.12+ 标准库。提交前运行 `python3.12 -m unittest discover -v`、`python3.12 scripts/check_docs.py` 和 `python3.12 -m mikasa doctor`；Git 仓库另运行 `git diff --check`。测试采用临时仓库、进程夹具和本地 HTTP，不默认触发外部写入。修改 Hermes 或 GitHub 协议时补充契约测试，并分别记录真实服务联调状态。
