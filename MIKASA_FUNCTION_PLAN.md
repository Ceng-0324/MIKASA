# 推进计划

目标：让 Mikasa 作为有持续身份、协作记忆和独立判断的程序员参与工程。Hermes 承载完整执行、工具和状态，CCH 提供模型路由，Mikasa 保留身份、skills、账号关系与必要适配。

1. **完整原生工程迁移已完成**：完整 Hermes 工程入口、持久 Git 工作区、共享记忆与原生会话/调度已部署 VM。旧 worker、快照、串行 runner 与发布业务引擎退休；GPT/Claude 真实合成工程和备份恢复通过，原生 gh 认证可用。具体工具与环境边界见验证记录。
2. **专用工作机与基础恢复已完成**：独立 OrbStack mikasa 机器内开放完整 sudo，移除服务文件/提权沙箱；关闭宿主共享、SSH Agent 和宿主/机器间网络。进程异常、机器重启恢复与日志策略通过；按实际项目补充依赖。OrbStack 共享内核且依赖 Mac，Mac 自启仍关闭；Docker Hub 拉取的网络问题尚待解决。
3. **聊天工程接入与真实仓库闭环已完成**：飞书会话通过 Hermes 原生工具在 FluxCore 独立 worktree 完成依赖修复、测试、构建、提交和 PR；`Mikasa-0910` 创建的 PR #28 已由负责人合并，进度与结果原生投递。外部工具采用原生依赖与配置，不编写替代 Agent。按负责人安排，不再单独安排查他人 PR、正式 Review 和 Review 修复验收；工程方法 skill 仍可按实际任务使用。
4. **交互与适配收敛**：保留逐工具进度、中途说明与 15 秒长任务提醒；跨会话接续使用 Hermes 历史检索、resume 和共享记忆。旧 HTTP 聊天、状态账本、命令适配和专属探针退役，不再维护第二套会话语义。Gateway 配置直接交给 Hermes 原生加载器，profile 初始化锁已收窄，终端与消息入口可并存，运行期并发和重复 Gateway 拒绝交给 Hermes 原生锁。

[VM 维护入口](deploy/vm/manage.py) 已接通源码版本、原生排空、加密备份与独立目录恢复，旧版回切和重部署已实测。接下来以真实使用中的具体任务检验稳定性，不继续扩建通用业务流程。CCH default 分组的管理端路由证据、外部服务工具和独立 24 小时主机按实际需要验证。每个可验证阶段创建本地提交，不自动推送。其他未验证能力见 [验证边界](docs/VALIDATION.md)。
