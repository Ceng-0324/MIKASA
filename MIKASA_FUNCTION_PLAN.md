# 推进计划

目标：让 Mikasa 作为有持续身份、协作记忆和独立判断的程序员参与工程。Hermes 承载完整执行、工具和状态，CCH 提供模型路由，Mikasa 保留身份、skills、账号关系与必要适配。

1. **完整原生工程迁移**：工程总入口、持久 Git 工作区、完整工具与扩展、共享记忆、原生会话/调度；退休旧 worker、快照、串行 runner 与发布业务引擎。代码迁移和备份兼容完成，正在进行 VM 真实工程验证。
2. **VM 稳定性与工程环境**：验证本轮原生工程能力，保留消息连续性；后续演练进程异常、VM 重启及日志策略。OrbStack 在线依赖 Mac，不能当作独立云主机。
3. **聊天工程任务与 FluxCore**：最后联合验收自然语言任务、进度反馈、真实仓库和 GitHub PR/Review。FluxCore 只是试点，不以其业务开发为前置。
4. **按实际使用继续收窄适配**：HTTP 聊天的鉴权、幂等、取消和调用方迁完后删除剩余兼容层；不为候选场景重新建立业务引擎。

CCH default 分组仍须网关管理端同次路由证据。GitHub 已确认 Mikasa-0910 身份，仓库写入与 Review 留到指定任务验收。每个可验证阶段创建本地提交，不自动推送。当前事实与未验证能力见 [验证边界](docs/VALIDATION.md)。
